# 復旧記録 2026-09-06 — phase93+ の喪失と zip からの復元

## 何が起きたか

- phase73〜90 は「1フェーズ=1コミット」を厳守していた(git reflog で確認可能)。
- **phase90 (`4f5393a`, 2026-09-01) 以降の作業(2026-09-03〜09-05)は一度も
  commit / push されていなかった。** 通称 "planexec + lns + la + tb135"。
  本番 public スコアを **57.894 → 58.498** に伸ばした実体。
- 2026-09-06、作業ディレクトリ `~/Desktop/baggage-packing` が空になっているのを発見
  (空の `.venv` のみ)。`git fetch origin` の結果 `origin/main` は phase90 のまま。
  この Mac に 09-03 以降のセッション transcript も無し。
- → **phase93+ のソース原本・レポート・narrative を喪失。**

## 復元したもの

### ソース(完全復元、本コミット `e8eb9d0` で main に反映)

唯一残っていた成果物:
`baggage-packing-backup-2026-09-02/submissions/mysolver_submit_w3_planexec_lns_la.zip`
(2026-09-03 16:30、sha256 `26f4903535b748d476cefaeb6a9d4962ff40f241d6e8260cf4922b960b650043`)
の中の `mysolver/*.py` をそのまま `agents/mysolver/` に展開した。

phase90 比の差分: `agent.py` +59 / `ordering.py` +259 / `planner.py` +144 / `simulate.py` +33 行。

同フォルダに `mysolver_submit_w3_planexec_lns.zip`
(2026-09-03 06:11、sha256 `dcf8fab04483a32b2b9fd6111e190949695f07825cfa285c8ff2b7a7a813a65d`)
= "lns" 段階(la の1つ前)も残存。

### 新規機構(いずれも env 既定 ON = 本番挙動そのもの)

| env | 既定 | 内容 |
|---|---|---|
| `MYSOLVER_PLAN_EXECUTOR` | `1` | `optimize()` が完全プラン(順序 + 各荷物の container_idx / local place_pos / orientation)を作り、`policy()` が毎ステップ計画済み値の合法性を検証して実機再現する。greedy 再計画をしない。 |
| `MYSOLVER_PLAN_EXECUTOR_GATE` | `lookahead` | plan-executor を有効化するシーンの判定。`shelf_prepacked`(棚あり or 既積みありで無効、Phase93 v3)/ `lookahead` / `none`。 |
| `MYSOLVER_PACK_LNS` | `1` | プラン後改善。remove-tail-K → 雑音付き repack、**配置数で受理**。`_lns_risky` でなく残り壁時計 > 12s のときのみ。 |
| `MYSOLVER_PACK_LNS_ITERS` | `18` | LNS 反復上限。 |
| `MYSOLVER_PACK_LNS_PER_STEP` | `1.5` | 1反復あたりの子予算(秒)。 |
| `MYSOLVER_PACK_LNS_NOISE` | `0.3` | repack 時の greedy_fill ノイズ。 |
| `MYSOLVER_TOPO_REORDER` | `0` | 搬入前後関係 DAG に基づくプラン並べ替え(既定オフ)。 |
| `MYSOLVER_PLAN_BUDGET_CUT` | `40.0` | full build でないときプラン生成に割く予算の削減量。 |
| `MYSOLVER_PLAN_WALL_FACTOR` | `1.0` | プラン生成の壁時計係数。 |

### tb135 の本番 zip(再構築、原本未発見)

`mysolver_submit_w3_planexec_lns_la_tb135.zip` の原本はディスク上に無い。
`_la` を展開し `ordering.py` の `DEFAULT_TIME_BUDGET = 120.0` を `135.0` に変えた1行差で再構築:

- 場所: `submissions/mysolver_submit_w3_planexec_lns_la_tb135.zip`(git 管理外 = .gitignore)
- 再構築版 sha256: `f03e6a98e43ebb86f63b6869662d56f22c4d209c8fe4da92f38a06898c8fb2b0`
- **原本 SHA と照合できないため、本番と厳密一致する保証は無い。** SIGNATE から
  提出済み zip を取得できれば要バイト照合。取れなければ再提出で public を確認。
- ユーザー判断(2026-09-06): 再構築版を正として進める。

## 本番提出の現況(2026-09-06、ユーザーが SIGNATE 画面で確認)

| 枠 | zip | public |
|---|---|---|
| 主枠 | `mysolver_submit_w3_planexec_lns_la_tb135.zip` | **58.49840707628679** |
| 2枠目 | `mysolver_submit_rest020.zip` | 55.95609315363339 |

## 回収できていないもの

- phase93〜(最終フェーズ番号不明)の各レポート `results/phaseNN_report.md`。
- `docs/submission_policy.md` の §14 以降(phase91 で §13 まで記載済み、それ以降の追記が消失)。
- plan-executor / PACK_LNS の A/B 実測値、失敗記録、パラメータ選定の経緯、
  tb135 に至る budget スイープの結果。
- `_la` より後の実験 zip(存在したかどうか自体も不明)。
- 参考: `~/Downloads/result-log (2)〜(9).txt`(2026-09-03〜05)に単シーン評価の断片が残る
  (fill ~38 / num_placed 0.644〜0.651 / optimization 112〜143s / policy 6.0〜6.5s、
  status は毎回 "Stopped in the middle. Did not satisfy {is_valid, is_placed_safe}")。

## 再発防止

- **各 phase の終わりに必ず commit + push。** phase73〜90 の運用に戻す。
- 作業ツリーは `~/dev/baggage-packing`(旧: `~/Desktop/baggage-packing`)。
- 退避: phase90+phase92 時点の `agents/mysolver` を `agents/mysolver.phase92_bak/`(git 管理外)に保持。
