"""
tools/gen_tech_doc.py

技術対策テキスト `docs/技術対策.docx` を生成する。

用途:
    面接などで本プロジェクトの技術内容を**口頭で**説明するための解説文書。
    README が「読ませる」ためのものなのに対し、こちらは「説明できるようになる」ためのもので、
    **平易さを最優先**する。具体的には次の方針で書く。

      1. 専門用語は必ずその場で噛み砕く(「充填率スコア(= どれだけ体積を詰められたか)」)。
      2. 数式より先に「たとえ話」を置く。
      3. 「なぜそれをやったか」を必ず1文で言えるようにする。
      4. 想定質問には、まず結論を1〜2文で答えてから補足する形にする。

運用ルール(README「開発運用ルール」に明記):
    新しいPhaseが完了したら、**docxを直接編集せずこのスクリプトを編集して再生成**する。
        python tools/gen_tech_doc.py

再生成は冪等(同じ内容なら同じ文書になる)。
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from docx import Document
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.shared import Pt, RGBColor

OUT = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
                   'docs', '技術対策.docx')

ACCENT = RGBColor(0x1F, 0x4E, 0x79)


def h1(doc, text):
    p = doc.add_heading(text, level=1)
    for r in p.runs:
        r.font.color.rgb = ACCENT
    return p


def h2(doc, text):
    return doc.add_heading(text, level=2)


def para(doc, text, bold=False, size=10.5):
    p = doc.add_paragraph()
    r = p.add_run(text)
    r.bold = bold
    r.font.size = Pt(size)
    return p


def bullet(doc, text, level=0):
    p = doc.add_paragraph(style='List Bullet' if level == 0 else 'List Bullet 2')
    r = p.add_run(text)
    r.font.size = Pt(10.5)
    return p


def qa(doc, q, a):
    p = doc.add_paragraph()
    r = p.add_run('Q. ' + q)
    r.bold = True
    r.font.size = Pt(10.5)
    r.font.color.rgb = ACCENT
    p2 = doc.add_paragraph()
    r2 = p2.add_run('A. ' + a)
    r2.font.size = Pt(10.5)
    p2.paragraph_format.space_after = Pt(10)


def table(doc, headers, rows):
    t = doc.add_table(rows=1, cols=len(headers))
    t.style = 'Light Grid Accent 1'
    for i, hd in enumerate(headers):
        c = t.rows[0].cells[i]
        c.text = ''
        run = c.paragraphs[0].add_run(hd)
        run.bold = True
        run.font.size = Pt(9.5)
    for row in rows:
        cells = t.add_row().cells
        for i, v in enumerate(row):
            cells[i].text = ''
            run = cells[i].paragraphs[0].add_run(str(v))
            run.font.size = Pt(9.5)
    doc.add_paragraph()
    return t


def build():
    doc = Document()
    st = doc.styles['Normal']
    st.font.name = 'Yu Gothic'
    st.font.size = Pt(10.5)

    # ---- 表紙 ----
    t = doc.add_heading('技術対策テキスト', level=0)
    t.alignment = WD_ALIGN_PARAGRAPH.CENTER
    p = doc.add_paragraph()
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    r = p.add_run('非凸コンテナ手荷物積付け最適化エージェント\n'
                  'NEDO Challenge コンテスト2「積付アルゴリズム」(SIGNATE)')
    r.font.size = Pt(12)
    p = doc.add_paragraph()
    p.alignment = WD_ALIGN_PARAGRAPH.CENTER
    r = p.add_run('最終更新: Phase28 時点')
    r.font.size = Pt(9)

    para(doc, 'この文書の使い方', bold=True)
    para(doc, 'この文書は「口で説明できるようになる」ことを目的にしています。'
              'READMEが読ませるための文書なのに対し、こちらは話すための文書です。'
              '専門用語はその都度かみ砕いてあります。'
              '面接前は §1(30秒版)→ §2(全体像)→ §7(想定質問)の順に読むのが最短です。')

    doc.add_page_break()

    # ---- 1. 30秒版 ----
    h1(doc, '1. まず30秒で言えるようにする')
    para(doc, '「空港の手荷物を、コンテナに自動で詰めるアルゴリズムのコンペです。'
              '物理シミュレータの上で、荷物を1個ずつ置いていきます。'
              '1回でも置き方を間違えるとその場で試合終了になるルールなので、'
              '“確実に置ける手だけを選ぶ”ことと“たくさん詰める”ことの両立が難しい問題でした。'
              'スコアは16.94点から53.64点まで上げました。』', bold=True)
    para(doc, 'ここまで言えれば、あとは相手の質問に応じて深掘りできます。')

    h2(doc, 'この問題の何が難しいのか(3つだけ覚える)')
    table(doc, ['難しさ', 'ひとことで言うと', 'なぜ厄介か'],
          [['非凸コンテナ',
            'コンテナが単純な箱ではない',
            '横に開口部があり、中に棚があり、角が斜めに切り欠かれている。'
            '「入るかどうか」の判定が単純な直方体の計算では済まない'],
           ['搬入経路(到達可能性)',
            '置き場所が空いていても、そこまで運べないと置けない',
            '荷物は必ず手前から押し込む。手前に高い荷物を置くと、'
            '奥の空間に行く通り道を自分で塞いでしまう'],
           ['サドンデス',
            '1手でも失敗したらそこで終了',
            '残りの荷物が全部無駄になる。'
            'だから「たぶん置ける」ではなく「確実に置ける」手しか出せない']])

    # ---- 2. 全体像 ----
    h1(doc, '2. システムの全体像')
    para(doc, 'プログラムは大きく2つのパートに分かれます。'
              'この2つの違いを説明できると、以降の話が全部つながります。')
    table(doc, ['パート', 'いつ動くか', '何をするか', 'たとえ'],
          [['optimize(オフライン最適化)',
            '試合開始前に1回だけ(最大180秒)',
            '全部の荷物を見て「どの順番で流すか」を決める',
            '試合前の作戦会議'],
           ['policy(オンライン配置)',
            '荷物1個ごとに毎回(1手8秒以内)',
            '今来た荷物を「どこに・どの向きで」置くか決める',
            '試合中の1手1手の判断']])
    para(doc, '重要な制約: policy のときは、次に来る荷物が1個しか見えないケースがあります'
              '(lookahead=1)。つまり「この荷物は後に取っておこう」ができない。'
              'だから順番を決める optimize が効いてきます。')

    h2(doc, 'ファイル構成(役割だけ言えればよい)')
    table(doc, ['ファイル', '役割'],
          [['agent.py', 'コンペ側と接続する窓口'],
           ['geometry.py', '幾何計算。「そこに入るか」「そこまで運べるか」を判定する'],
           ['planner.py', '1手を決める中核。候補地点を作って点数をつけて一番良いものを選ぶ'],
           ['ordering.py', '荷物を流す順番を決める(オフライン最適化)'],
           ['simulate.py', '影シミュレータ。物理を使わず高速に「この順番なら何個置けるか」を試算する']])

    # ---- 3. 中心的な考え方 ----
    h1(doc, '3. 中心にある3つの考え方')

    h2(doc, '(1) 影シミュレータ ― 物理を使わずに順番を試す')
    para(doc, '順番を変えて試したいのですが、本物の物理シミュレータは重くて何百通りも試せません。'
              'そこで「物理演算を使わず、幾何計算だけで置けるかどうかを判定する軽い模擬装置」を作りました。'
              'これを影シミュレータと呼んでいます。')
    para(doc, 'ポイント: 本番と同じ判定ロジックを使うので、'
              '「本番でも置けるはず」という予測がかなり正確になります。'
              '実測では、どの荷物が置けるかの予測誤差はほぼゼロでした。')

    h2(doc, '(2) 決定性 ― 同じ入力なら必ず同じ答え')
    para(doc, 'もともとは「制限時間まで探索する」という作りでした。'
              'これだとCPUの混み具合で答えが変わってしまい、'
              '改善したのか運が良かったのか区別できませんでした。')
    para(doc, 'そこで打ち切り条件を「時間」から「探索した量(ユニット)」に変えました。'
              'たとえるなら、「10分間歩く」ではなく「1万歩あるく」に変えたということです。'
              '足の速さ(CPUの速さ)が変わっても、結果は同じになります。')
    para(doc, '効果: 同じ条件で5回実行したときのブレが 5.25 → 0.0000 になりました。'
              'これ以降、改善の判定が一気にやりやすくなりました。', bold=True)

    h2(doc, '(3) ノイズ床 ― 「意味のある差」の基準を先に決める')
    para(doc, '測定には必ずブレがあります。そこで「この幅より小さい差は意味がない」という基準'
              '(ノイズ床)を先に測っておきました。充填率なら ±0.90点です。'
              '改善したかどうかは、結果を見てから決めるのではなく、この基準で機械的に判定します。')

    # ---- 4. 代表的な成果 ----
    h1(doc, '4. 代表的な成果(面接で1つ話すならこれ)')

    h2(doc, '搬入経路の自己封鎖と、物理から導いた「不感帯」')
    para(doc, '問題: 体積が2〜3割しか埋まっていないのに詰まる。調べると、'
              '「奥に行く通り道を自分で塞いでいた」ことが原因でした。')
    para(doc, '対策: 「自分より奥にあるものより高く積んだら減点」というルールを入れました。'
              'ところが最初はまったく効かず、むしろ結果がガタガタになりました。')
    para(doc, '原因: 実は「少しくらい高くても、まだ通り道は生きている」範囲があったのです。'
              'そこまで減点していたので、無意味に判断を揺さぶっていました。')
    para(doc, '解決: その「セーフな高さ」を物理的な数値から計算しました。', bold=True)
    para(doc, '    不感帯 = 0.016(置くときの浮かせ量) + 0.08(運ぶときの持ち上げ量) '
              '− 0.0155(すり抜けに必要な余裕) = 0.0805 m')
    para(doc, 'これは「ちょうど良い値を探した」のではなく、'
              '3つの物理定数から一意に決まる値です。'
              'これを入れた瞬間、応答がなめらかになり、狙いどおり改善しました。'
              '実際に詰まりの発生率が 22.5% → 3.1% に下がっています。')

    # ---- 5. Phaseごとの物語 ----
    h1(doc, '5. Phaseごとの物語(何を疑い、何が分かったか)')
    para(doc, 'この節が本プロジェクトの中身です。'
              '「うまくいった話」だけでなく「間違いに気づいた話」も入れてあります。')

    h2(doc, 'Phase10 ― 自分の評価環境を疑った')
    para(doc, '6つのシーンだけで開発していたが、本番の点数と噛み合わなくなってきた。'
              '26シーンの評価セットを新たに作って測ったら、'
              '旧6シーンは「簡単すぎるシーン」ばかりだったと判明。'
              '以降はこの26シーンで判定するようにした。')

    h2(doc, 'Phase11 ― 床に置いた荷物が点数に入っていなかった')
    para(doc, '「置いた荷物のうち3〜4割が充填率に計上されていない」ことを発見。'
              '原因は、床にぴったり接している荷物が判定式の都合で'
              '「はみ出している」と扱われるためだった。')

    h2(doc, 'Phase12〜13 ― 自分の結論が循環論法だったと気づいて撤回した')
    para(doc, '一度は「本番の判定基準はこちらだ」と結論づけたが、'
              'その根拠が自分たちの過去の計測値そのものだった(同じ基準で測った値を'
              '同じ基準で測り直して一致するのは当たり前)。'
              '誤りと判断して撤回し、以降は「どちらの基準でも良くなる変更だけ採用する」方針にした。')

    h2(doc, 'Phase17 ― 決定性を手に入れた')
    para(doc, '打ち切りを時間ベースからユニットベースに変更(§3-(2))。'
              'ブレが 5.25 → 0.0000 になり、以降のすべての実験の信頼性が上がった。')

    h2(doc, 'Phase18 ― 1つの重みでは調整しきれないと分かった')
    para(doc, '安定性を目的関数に入れようとしたが、'
              'あるシーンを直す重みが別のシーンを壊し、両立する値が存在しなかった。'
              '「重みを1つ足して調整する」というやり方の限界を確認して不採用にした。')

    h2(doc, 'Phase20 ― 「較正」と「弁別」は別物だと分かった')
    para(doc, '影シミュレータの予測が実際とどれだけズレるかを125通り測定。'
              '予測は系統的に甘めだったので、物理的に正しい計算に直したところ'
              '**ズレは大幅に改善したのに、結果は1ミリも良くならなかった**。')
    para(doc, '理由: 探索が使っているのは「候補どうしの順位」だけ。'
              '全部の候補が同じだけ甘く出ていても、順位は変わらないからです。'
              'たとえるなら、全員のテストの点を10点ずつ上げても順位は変わらないのと同じ。')
    para(doc, 'この発見により「予測を正確にする」という方向性自体を打ち切りました。', bold=True)

    h2(doc, 'Phase21 ― 「伸びしろ」だと思っていたものが実は制約だった')
    para(doc, '「計上されていない3〜4割」を回収すれば大きく伸びると考えていた。'
              '574個の荷物を全数調査したところ、**96.4%が「床に接している」という構造的な理由**で、'
              '傾きや棚との接触は0件だった。')
    para(doc, '床の層は積み上げの土台なので無くせない。'
              '計算すると目標値は幾何学的に到達不能で、'
              '「伸びしろ」ではなく「指標の性質」だったと結論した。')

    h2(doc, 'Phase22 ― 自分の解釈を、採用直前に自分で否定した')
    para(doc, '空きスペースを分解したところ、強い探索なら追加で置ける分が見つかった。'
              '当初これを「探索の細かさが足りないせい」と解釈し、'
              '細かさを上げる変更を検証にかけた。')
    para(doc, 'しかし採用を決める前に内訳を分けたところ、'
              '**細かさは関係なく(上げても1立方センチも増えない)、'
              '本当の原因は「どの荷物を置くか選べる自由度」だった**と判明。'
              '26シーンでも改善0・悪化3で、不採用にした。')
    para(doc, 'この切り分けをせずに結果だけ見ていたら「効果なし」で終わり、'
              '次にどこを直せばよいかは分からないままでした。', bold=True)

    h2(doc, 'Phase23 ― 絞り込んだ1点に手を入れたら、素直に効いた')
    para(doc, 'Phase20と22の測定が同じ場所を指していた: '
              '「候補の選び方は良い。候補の作り方が悪い」。'
              'そこで順番を決める探索を、1本の貪欲法から'
              '**ビームサーチ**(有望な候補を同時に複数本たどる探索)に広げた。')
    para(doc, '結果: 26シーンで充填率 23.74 → 25.68(+1.95点)。'
              '本番スコア換算で +0.87点の改善。', bold=True)
    para(doc, '設計の勘所: 素直に作るとコストが数倍になり、'
              'Phase22で確認済みの「コストを増やすと逆効果」を繰り返すところでした。'
              '探索が元々すべての候補を調べていることを利用し、'
              '「荷物ごとの一番良い置き方をメモしておくだけ」にすることで、'
              'コストをほぼ増やさずに複数候補を得られるようにしました。')
    para(doc, 'なお、ここでも当初の見立ては外れました。'
              '「取りこぼし分を回収する」つもりでしたが、'
              '取りこぼしは在庫のように汲み出せる量ではなく、'
              'たくさん積んだ後の新しい状況で同じくらい再発生する量でした。')

    h2(doc, 'Phase24 ― 自分の過去の測定を測り直したら、4割の見落としが出た')
    para(doc, '最後に残っていたのが「荷物を入れる通り道が塞がれて使えない空間」です。'
              'Phase22ではこれを「空きの26.7%」と測っていましたが、'
              'その測定は自分で**簡略化している**と断り書きを残していました'
              '(荷物は搬入時に8cm持ち上がるのに、その分を無視していた)。'
              'そこでまず、本番の判定プログラムとまったく同じ式まで作り直しました。')
    para(doc, '結果: 通り道の封鎖は空きの26.7%ではなく**38.8%**。'
              '**過去の自分の測定は4割過小評価していました。**'
              'これで「空きの最大の原因は通り道の封鎖」だと確定しました。', bold=True)
    para(doc, 'さらに封鎖されている空間を分解すると、'
              '「高いところ・奥」に偏っていること(高さ上位3割に半分が集中)、'
              '**その4割は「荷物が持ち上がらない高さ帯」**にあることが分かりました。'
              '現在の設定はその帯にも8cmの猶予を与えていて、物理的に間違っています。')
    para(doc, 'そこでその猶予を高さ帯ごとに正しく計算し直したのですが、'
              '26シーンでの改善は+0.27点でノイズの範囲内、'
              'しかも改善したのは26シーン中1シーンだけでした。'
              '事前に決めた基準どおり**不採用**にしています。')
    para(doc, 'ただし今回は「なぜ効かないか」まで実行中に数えました。'
              'この仕組みは**すでに置いてある荷物の上の通り道しか守れない**のに対し、'
              '実際に塞がれている通り道の多くは**その後の積み上げで初めて生まれる**。'
              'つまり仕組み自体の限界であって、設定の精度の問題ではなかった。'
              'この特定により、次に手を入れるべき場所'
              '(積み終わった後の状態を見られる「順番を決める探索」の側)が確定しました。', bold=True)

    h2(doc, 'Phase25a ― 「採用」の判断そのものが間違っていたと気づいた話')
    para(doc, 'Phase23のビームサーチは、26シーン平均で充填率+1.95点という'
              'はっきりした改善だったので採用しました。'
              'ところが実際にコンペに提出した結果は**−0.45点の悪化**でした。'
              '「26シーンの平均を見て採否を決める」というやり方自体を疑いました。')
    para(doc, '仮説: 平均だけでなく、シーンごとのばらつきを見れば'
              '「本番の別のシーンでも通用するか」を事前に見分けられるはずだ。'
              'ばらつきが大きい変更は、一部のシーンで大きく良くなり'
              '一部のシーンで大きく悪くなっているのを平均で相殺しているだけかもしれない。')
    para(doc, '検証: シーンごとの差を統計処理し、'
              '「ばらつき(標準偏差)」「平均のブレ幅(標準誤差)」「t値」を出すようにしました。'
              'これはビジネスのA/Bテストで使う考え方と同じです。'
              'Phase23のデータをこの基準で計算し直すと、'
              't値は1.62(目安の2を下回る)でした。'
              'つまり統計的には「まだ自信を持って採用してよい水準」ではなかったのです。', bold=True)
    para(doc, '念のため、探索の予算を増やす別の変更(UNITS_PER_SEC)も同じ基準で試しましたが、'
              'こちらもt値0.68で不採用でした。'
              'タイムアウトなどの危険は一切起きておらず(安全確認は別途実施済み)、'
              '不採用の理由は「危ないから」ではなく'
              '「ばらつきが大きく、本番でも同じ結果になる保証がないから」です。')
    para(doc, '結果: 「26シーン平均が良くなったら採用」という基準を、'
              '「平均に加えて、ばらつき(標準偏差・標準誤差・t値)も必ず確認する」基準に変えました。'
              'ビームサーチ導入前の設定に一度戻し、この新しい基準の出発点としています。'
              '**採用の判断基準そのものを、実際の失敗から学んで作り直した**フェーズです。', bold=True)

    h2(doc, 'Phase26 ― 中間の「良い結果」が、実は自分の選んだサンプルのせいだった話')
    para(doc, '探索の予算を増やす方向は、コンペへの提出で直接確かめた結果'
              '(予算を1.55→2.0→2.5→3.0と増やすと53.61→53.64→53.42→53.39)、'
              'すでに頭打ちで、増やすとむしろ悪化することが分かっていました。'
              'そこで「探索の量」ではなく「詰め方そのもの」を変える案として、'
              'コンテナの奥から手前へ壁を1枚ずつ積んでいく方式(壁積み)を試しました。')
    para(doc, '実はこれに近いことは開発初期(Phase9)に一度試して失敗しています。'
              'ただし失敗の理由は記録してあって、'
              '「層の厚みを荷物のサイズと無関係な固定値にしたので、'
              '大きい荷物が層に入らず、小さい荷物だけが奥を埋めてしまった」でした。'
              '今回は層の厚みを**その場の荷物サイズの分布から決める**ようにして、'
              'この失敗の原因を構造的に潰した上で再挑戦しました。')
    para(doc, 'ここで自分が誤りを犯しました。26シーン全部を測る前に'
              '時間節約のため4シーンで方向性を確認したのですが、'
              'その4シーンを「変更の影響が一番大きいシーン」で選んでしまったのです。'
              '結果は+3.99点と強い改善に見えました。'
              'ところが26シーン全体では**−2.96点(t値 −2.29)**、'
              'つまり**符号ごと逆転して、統計的にはっきりした悪化**でした。', bold=True)
    para(doc, '原因は明快です。影響が大きいシーン=小さい荷物が多いシーンであり、'
              'それはちょうど「壁積みが得意なシーン」でした。'
              '実際、26シーンで大きく悪化した4シーンは'
              'サンプルに1つも入っていませんでした。'
              '**サンプルは「影響があるかどうか」で絞るべきで、'
              '「影響が大きいかどうか」で絞ってはいけない**、というのが教訓です。', bold=True)
    para(doc, 'なお失敗の内訳を層別すると、'
              '「壁積みが働かないはずのシーン群」が最も悪化しているという'
              '妙な結果が出て、これが実装のバグを直接指してくれました'
              '(層の厚みを「その時点で残っている荷物」から計算していたため、'
              '大きい荷物から先に置かれると終盤に層が勝手に薄くなり、'
              'Phase9とまったく同じ失敗が再発していた)。'
              'ただし影響を受けていないシーン群でも符号は負だったので、'
              'バグを直しても勝てる見込みはないと判断して打ち切りました。')
    para(doc, '結果: 不採用。ローカルで有意な改善が出ていないので、'
              '**コンペへの提出は行っていません**。'
              'Phase23で「ローカルの数字を見誤って提出し悪化させた」失敗があるので、'
              '今回は自分で決めた基準どおり提出前に止めました。')

    h2(doc, 'Phase27 ― 「7点分の宝の山」を、一度も計測せずに閉じた話')
    para(doc, '前のフェーズの副産物として、'
              '「同じ詰め方を厳しい物差しで測った点数」と'
              '「甘い物差しで測った点数」の差が12.18点あることが分かりました。'
              '本番スコアに換算すると7.3点分で、目標まで残り6.4点だったので、'
              'これ一つで届く計算になります。'
              'しかも前フェーズの実験でこの差が動くことも確認できていたので、'
              '「操作できる量なら取りに行けるはずだ」と考えました。')
    para(doc, '調べるために、荷物一つ一つの最終姿勢と'
              'コンテナの各面までの距離をすべて記録する仕組みを作る計画でした。'
              'ただし着手する前に一つだけ、'
              '**「配布されている公式の採点コードが、そもそも何と決めているか」を'
              '自分たちの採点コードと突き合わせる**ステップを最初に置きました。', bold=True)
    para(doc, 'この照合だけで、全部の疑問に答えが出ました。')
    bullet(doc, '自分たちの採点コードは独自の判定を持っておらず、'
                '公式のコードをそのまま呼んでいた(食い違いは無かった)')
    bullet(doc, '公式の判定対象には「コンテナの床面」も含まれていて、'
                '点数に入るには荷物が床から5mm浮いている必要がある。'
                '物理シミュレーションでは荷物は必ず床まで落ちるので、'
                '**床に置いた荷物は原理的に永久に点数に入らない**')
    bullet(doc, 'これは「荷物が床にめり込むから」ではない。'
                'めり込みがゼロでも、床にぴったり接した瞬間に除外される。'
                '判定式の引き算の問題であって、物理の問題ではなかった')
    bullet(doc, '決定打: 公式コードでは、この「物差しの厳しさ」の設定が'
                '**採点と「置いてよいかの判定」の両方を同時に動かす**。'
                'つまり厳しい世界では回収不能、甘い世界では最初から点数に入っている。'
                '**どちらの世界でも、取りに行ける宝の山は存在しなかった**')
    para(doc, '結果: 計装のコードは1行も書かず、計測も1回も実行せずにフェーズが終わりました。'
              '約1時間の計測と数百行のコードが不要になりました。'
              '「測って確かめる」より前に「仕様として何が決まっているか読む」ステップを'
              '置くだけで、丸ごと消える作業がある、というのがこのフェーズの学びです。', bold=True)
    para(doc, 'なお調べる過程で、このフェーズを始めたときの前提だった'
              '「この課題は未着手」「過去の記録同士が矛盾している」が'
              '**どちらも事実誤認**だったことも分かりました。'
              '実際にはPhase21が26シーン574件を全数調査して既に決着させており、'
              '記録も一貫していました。'
              '**思い込みで作業を始めず、まず記録を読み直す**ことの重要性も同時に確認できました。')

    h2(doc, 'Phase28 ― 「1シーンだけ大きく良くなった」は採用してはいけない、と数式で分かった話')
    para(doc, '空きスペースの38.8%は「置ける場所はあるのに、そこまで運ぶ通り道が塞がっている」'
              '空間でした。これが最後に残った最大の課題です。'
              '前のフェーズで「1手ごとの減点では原理的に守れない」ことが分かっていました。'
              '減点は「今ある荷物」しか見られませんが、通り道を塞ぐのは'
              '「これから積む荷物」だからです。')
    para(doc, 'そこで、順番を最後まで試し終えた**後**で'
              '「その順番が結果的に自分で通り道を塞いだか」を測り、'
              '塞いだ順番の評価を割り引くようにしました。'
              '設計では過去の失敗を3つ踏まえています: 減点ではなく割引にする'
              '(過去に「何も置かないのが一番得」という壊れ方をしたため)、'
              '移植したコードが元と完全に一致することを検証する、'
              'かかる時間を「時間」ではなく「計算量」で計上する(結果を毎回同じにするため)。')
    para(doc, '結果: 26シーン中 **1シーンだけ** が+9.10点良くなり、'
              '**残り25シーンは1ビットも変わりませんでした**。'
              '平均は+0.35点で、採用基準のt値は 1.000 でした。', bold=True)
    para(doc, 'ここで気づいたことがあります。**このt値=1.000は偶然ではありません。**'
              '25シーンが0で1シーンだけがdだけ動くとき、'
              '平均も標準誤差もどちらも d/26 になるので、'
              '**t値はdの大きさに関係なく必ず1.000になります**。'
              'つまり「1シーンだけしか動かない改善」は、'
              'そのシーンでどれだけ大きく改善しても、採用基準(t>2)を'
              '**原理的に通過できない**のです。', bold=True)
    para(doc, 'これは基準の欠陥ではありません。'
              '「1シーンだけの改善は、本番の別のシーンでも通用する根拠にならない」という'
              '基準の設計意図が、そのまま働いた結果です。'
              '改善幅の大きさに目を奪われる前に、**何シーンが動いたか**を'
              '先に見るべきだ、という教訓を得ました。')
    para(doc, 'なぜ25シーンが動かなかったかも突き止めました。'
              '「採用される順番が入れ替わる最小の重み」をシーンごとに測ると、'
              '**0.23 / 0.39 / 1.04 / 2.29 と10倍もばらついていました**。'
              'しかも一番小さいシーンは重み1.0で既に壊れ始めます。'
              'つまり「広く効く重み」と「どのシーンも壊さない重み」に'
              '**共通の範囲が存在しません**。過去に別の施策で同じ壁に当たっており、'
              'それが運ではなく構造だと確定しました。')
    para(doc, 'さらに根本的な発見として、この評価軸で順番が入れ替わるとき、'
              '選ばれるのは**必ず「置いた個数が少ない」順番**でした(21個→4個など)。'
              '荷物を1個置くたびに通り道を塞ぐ可能性が生まれるので、'
              '**「通り道を開けておく」と「たくさん詰める」は原理的に対立します**。'
              'この対立はこの方向の打ち手すべてに共通する制約です。', bold=True)

    # ---- 6. 開発の進め方 ----
    h1(doc, '6. 開発の進め方(ここが一番のアピールポイント)')
    para(doc, 'このプロジェクトはAI支援開発です(指示作成に Claude for Mac、'
              '実装・計測に Claude Code)。したがってアピールすべきは'
              '「コードを書く力」ではなく「調べ方・決め方の規律」です。', bold=True)
    table(doc, ['やっていること', '具体的に', 'なぜ効くか'],
          [['判定基準を先に決める',
            '「26シーンで±0.90点を超えたら採用」を結果を見る前に固定',
            '都合の良い解釈を後付けできなくなる'],
           ['測定のノイズを先に測る',
            '9シーン×5回でブレ幅を定量化',
            '「改善した気がする」を排除できる'],
           ['2段階で検証する',
            'まずブレない少数シーンで方向性を確認 → 26シーンで最終判定',
            '時間を節約しつつ過学習を防げる'],
           ['1回に1つしか変えない',
            '複数の定数を同時に変えるのを禁止',
            '効いた原因を特定できる。実際に'
            '「2つ同時に変えたときだけ壊れる」現象を発見した'],
           ['閉じた方向を記録する',
            '棄却した仮説を理由つきで残す',
            '同じ失敗を繰り返さない。次の担当者も迷わない']])

    para(doc, '実績: この規律は実際に誤った採用を複数回防いでいます。'
              'Phase18(少数シーンでは成功に見えた変更を26シーンで却下)、'
              'Phase22(採用直前に自分の解釈を否定)、'
              'Phase24(改善が26シーン中1シーンだけだったため却下)、'
              'Phase26(中間の4シーンでは+3.99点だったが26シーンでは−2.96点で却下)、'
              'Phase27(実装前に公式仕様を読み直し、計測ゼロで方向ごと却下)、'
              'Phase28(1シーンしか動かず、t値が原理的に基準を通過できないと判明し却下)。'
              '一方Phase23は、この規律をすり抜けて誤って採用してしまった例です'
              '(26シーン平均は改善に見えたが実際の提出は悪化)。'
              'Phase25aでその失敗を統計的に検証し、'
              '「平均だけでなくばらつきも見る」という基準を追加しました。'
              '**基準そのものにも間違いはあり得る、というのが正直なところです。**')

    # ---- 7. 想定質問 ----
    h1(doc, '7. 想定質問と答え方')

    qa(doc, 'AIが書いたコードなら、中身を理解しているのですか?',
       '理解している部分と、そうでない部分を分けてお答えします。'
       '個々の実装の細部まで暗記しているわけではありません。'
       '一方で「なぜその変更をしたのか」「何を測って採否を決めたのか」は'
       'すべて自分の判断です。たとえば搬入経路の不感帯0.0805mは'
       '3つの物理定数から導かれる値で、なぜその値かを説明できます。'
       'また、AIが提案した施策を測定結果に基づいて却下した例も複数あります。')

    qa(doc, 'スコアが伸び悩んだ時期はどうしましたか?',
       'Phase20から22の3フェーズは、スコアがほとんど動きませんでした。'
       'ただしその間は「どこを直せば効くか」を消去法で絞り込んでいました。'
       '具体的には、予測精度の改善は効かない、計上率は独立に動かせない、'
       '取りこぼしは順番に起因する、と順に確定させました。'
       'Phase23でその1点に手を入れたら+1.95点出たので、'
       '止まっていた期間の測定は無駄ではなかったと考えています。')

    qa(doc, '一番苦労した点は?',
       '「改善したかどうかが分からない」ことでした。'
       '当初は同じ条件で実行しても結果が毎回変わり、'
       '改善なのか偶然なのか区別できませんでした。'
       '原因は探索の打ち切りが実行時間に依存していたことで、'
       'これを探索量ベースに変えてブレをゼロにしました。'
       'ここを直してから、以降の実験の判断が一気に速くなりました。')

    qa(doc, '失敗した施策を教えてください',
       '複数あります。代表例を1つ挙げると、'
       '影シミュレータの予測が甘いと分かったので物理的に正しい計算に直したのですが、'
       '予測精度は大きく改善したのに結果は全く変わりませんでした。'
       '調べると、探索は候補どうしの順位しか使っておらず、'
       '全候補が同じだけ甘く出ていても順位は変わらないためでした。'
       'この件から「精度を上げる」のではなく'
       '「候補の作り方を変える」方向に切り替え、それが最終的に+1.95点につながりました。')

    qa(doc, 'なぜ順位相関という指標を使ったのですか?',
       '探索が実際に使っている情報がそれだけだからです。'
       '探索は候補を点数で並べて一番良いものを選ぶので、'
       '点数の絶対値がどれだけ正確でも、並び順が同じなら選ぶ結果は変わりません。'
       '最適化器が何を使っているかから逆算して測定対象を決めました。')

    qa(doc, '採用した施策が後から間違いだったと分かったことは?',
       'あります。ビームサーチという探索の改善を、26シーン平均で+1.95点の改善を'
       '確認して採用しましたが、実際にコンペに提出すると−0.45点悪化していました。'
       '原因を調べると、シーンごとの効果が+11点〜−15点まで大きくばらついており、'
       '平均のプラスは一部シーンの大幅な改善と一部シーンの大幅な悪化が'
       '相殺していただけでした。この経験から、平均だけでなく'
       'ばらつき(標準偏差・標準誤差・t値)も必ず確認する基準に変更しました。'
       '採用基準そのものも、間違っていたと分かれば直す対象だと考えています。')

    qa(doc, 'この経験を業務にどう活かせますか?',
       '「測れないものは改善できない」ことと、'
       '「測る対象を間違えると改善しても意味がない」ことを、'
       '実例で経験できたことが一番の収穫です。'
       '施策を打つ前に判定基準とノイズ幅を決めておく、'
       '効かなかったときに原因を切り分けて次の一手に繋げる、'
       'といった進め方はそのまま業務に持ち込めると考えています。')

    # ---- 8. 数字 ----
    h1(doc, '8. 覚えておく数字')
    table(doc, ['項目', '数字', 'ひとこと'],
          [['提出スコア', '16.94 → 51.00', 'ベースラインから約3倍'],
           ['充填率(現在)', '24.41', '26シーン平均(測定用の探索予算1.55e7時)。'
                                     'Phase25aでビームサーチ導入前の設定に戻して以後の基準点にした'],
           ['体積利用率', '39.5%', 'コンテナ容積のうち実際に詰められた割合(採用時点)'],
           ['実行のブレ', '5.25 → 0.0000', 'Phase17の決定性リファクタ'],
           ['ノイズ床', '±0.90点', '同じシーンを再実行したときのブレの基準'],
           ['採用基準のt値', 't > 2', 'Phase25aから追加。シーンごとの効果のばらつきを'
                                      '考慮した基準(t=1.62のPhase23は本来この基準では不採用)'],
           ['評価シーン数', '26', 'パターン・コンテナ数・棚・既積みを網羅'],
           ['不感帯', '0.0805 m', '搬入経路の保護。物理定数3つから導出'],
           ['計上漏れの内訳', '96.4%が床接地', '傾き・棚接触は0件'],
           ['通り道の封鎖', '空きの38.8%', 'Phase24で厳密に測り直した。'
                                           '過去の自分の測定は4割過小評価だった'],
           ['壁積みの結果', '−2.96点(t=−2.29)', 'Phase26。中間4シーンでは+3.99点に'
                                                 '見えたがサンプルの選び方の誤りだった'],
           ['厳/緩の物差しの差', '12.18点', 'Phase27。7.3点分の宝の山に見えたが、'
                                            '床置きは原理的に点数に入らないと判明し閉じた'],
           ['単一シーン効果のt値', '恒等的に1.000', 'Phase28。1シーンだけ動く変更は'
                                                   '改善幅によらずt=1.000で、基準t>2を'
                                                   '原理的に通過できない']])

    os.makedirs(os.path.dirname(OUT), exist_ok=True)
    doc.save(OUT)
    print(f'wrote {OUT}')


if __name__ == '__main__':
    build()
